targets:
  $default:
    sources:
      - lib/**
      - "tool/**"
      - pubspec.yaml
      - lib/$lib$
      - $package$
    builders:
      drift_dev:
        options:
          databases:
            twonly_db: lib/src/database/twonly.db.dart
            twonly_database: lib/src/database/twonly_database_old.dart